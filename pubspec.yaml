name: financa_plus
description: Finança+ - Controle Financeiro, Educação e Investimento

publish_to: 'none' # Este projeto não será publicado no pub.dev

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  cloud_firestore: ^4.9.0
  firebase_core: ^2.24.0
  firebase_auth: ^4.17.0
  charts_flutter: ^0.12.0
  intl: ^0.17.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
